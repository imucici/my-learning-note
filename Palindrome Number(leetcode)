class Solution:
    def isPalindrome(self, x: int) -> bool:
        b = str(x) # 先將int轉成字串型態
        c = b[::-1] # [::-1]將字串順序反轉 
        if c == b :
            return True
        else:
            return False
